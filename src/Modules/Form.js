import {} from "./DefaultTypes"
import Species from "./Species";

class Form extends Species {
    constructor
        (name, id, ) {
            this.name = name;
            this.id = id;
            
    }
}

export default Form